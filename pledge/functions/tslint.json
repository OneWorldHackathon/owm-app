{
  "extends": "tslint-config-airbnb",
  "rules": {
    "semicolon": [true, "never"],
    "variable-name": [
      true,
      "ban-keywords",
      "check-format",
      "allow-leading-underscore"
    ],
    // "arrow-parens": true,
    // todo this needs removing and correcting
    "prefer-template": [
      false
      // "allow-single-concat"
    ]
  }
}
